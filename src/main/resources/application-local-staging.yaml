fint:
  org-id: fintlabs.no
  flyt:
    gateway:
      application:
        archive:
          client:
            fint-flyt-file:
              base-url: http://localhost:8091
            fint-archive:
              base-url: https://beta.felleskomponent.no
              authorization:
                enable: true

  kafka:
    default-replicas: 1
spring:
  kafka:
    bootstrap-servers: localhost:9092
server:
  port: 8301
